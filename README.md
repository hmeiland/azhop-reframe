# Reframe tests for Az-HOP

This repo hosts the Reframe tests and config file for the Az-HOP environment.
To use:

```
#> git clone
#> cd azhop-reframe
#> source EESSI
#> reframe -C config/settings.py --system azhop -c checks/microbenchmarks/cpu/ --recursive -r
```
